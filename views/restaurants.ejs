<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("includes/head") %>
    <link rel="stylesheet" href="/styles/restaurants.css" />
  </head>
  <body>
    <%- include("includes/header") %> <%- include("includes/mobile-drawer") %>
    <main>
      <h1>Recommended restaurants</h1>
      <p>Find your next favorite restaurants with help of our other users!</p>
      <p>We have <%= length %> restaurants shared.</p>
      <form action="/restaurants" method="get">
        <div>
          <input class="btn" type="submit" name="order" value="asc" />
          <input class="btn" type="submit" name="order" value="desc" />
        </div>
      </form>
      <% if (length == 0) {%>
      <p>
        Currently there is no restaurant shared, please share your favorite
        restaurants.
      </p>
      <% } else { %>
      <ul id="restaurants-list">
        <% for (const restaurant of restaurants) { %>
        <li class="restaurant-item">
          <%- include("includes/restaurants/restaurant-item", {restaurant:
          restaurant}) %>
        </li>
        <% } %>
      </ul>
      <% } %>
    </main>
  </body>
</html>
